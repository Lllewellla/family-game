# Быстрый старт FamilyQuest

## Репозиторий

Проект подключен к: **https://github.com/Lllewellla/family-game**

## Шаг 1: Клонирование (если нужно)

```bash
git clone https://github.com/Lllewellla/family-game.git
cd family-game
```

## Шаг 2: Первый коммит

Если вы только что создали проект локально:

```bash
# Проверить статус
git status

# Добавить все файлы
git add .

# Создать коммит
git commit -m "Initial commit: FamilyQuest implementation"

# Отправить в репозиторий
git push -u origin main
```

Если ветка называется `master`:
```bash
git branch -M main
git push -u origin main
```

## Шаг 3: Настройка Backend

1. Создайте файл `backend/.env` на основе `backend/.env.example`
2. Заполните все переменные окружения
3. Задеплойте на Railway (см. `DEPLOYMENT.md`)

## Шаг 4: Настройка Frontend

1. Скопируйте `frontend/` в `docs/`:
```bash
mkdir -p docs
cp -r frontend/* docs/
```

2. Обновите `docs/config.js` с URL вашего Railway backend

3. Закоммитьте:
```bash
git add docs/
git commit -m "Add frontend for GitHub Pages"
git push
```

4. Включите GitHub Pages в настройках репозитория (Settings → Pages → Source: /docs)

## Шаг 5: Настройка Telegram Bot

1. Создайте бота через @BotFather
2. Настройте Menu Button на URL вашего GitHub Pages
3. Добавьте токен бота в переменные окружения Railway

## Готово! 🎉

Теперь можно открыть бота в Telegram и начать использовать FamilyQuest.

## Полезные команды

```bash
# Проверить статус
git status

# Посмотреть изменения
git diff

# Добавить изменения
git add .
git commit -m "Описание изменений"
git push

# Обновить с удаленного репозитория
git pull
```

## Дополнительная документация

- `DEPLOYMENT.md` - Подробная инструкция по деплою
- `PROJECT_STRUCTURE.md` - Структура проекта
- `GIT_SETUP.md` - Настройка Git
